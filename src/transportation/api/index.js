import * as price from './fake.api/price'

const API = {
    price
}
export default API

// console.log('API.price', API.price.fetchAllCars().)
// console.log('API.price.fetchAll()', API.price.fetchAll())
